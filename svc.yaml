apiVersion: v1
kind: Service
metadata:
  name: myflask-svc
  namespace: keda-demo
spec:
  selector:
    app: myflask
  ports:
  - name: http
    port: 80
    targetPort: 5334
    protocol: TCP
  type: ClusterIP
